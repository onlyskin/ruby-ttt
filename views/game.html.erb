<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>TTT</title>
  	<style>
      body {
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      .container-fluid {
        display: flex;
        flex-direction: column;
      }

      .row {
        display: flex;
      }

      .board-cell {
        width: 100px;
        height: 100px;
        font-size: 30px;
        border: 1px solid black;
        background: -webkit-linear-gradient(top, #fcecfc 0%,#fba6e1 50%,#fd89d7 51%,#ff7cd8 100%);
      }
  	</style>
  </head>
  <body>
		<div class="container-fluid">            
      <% (0..2).each do |row| %>
		  <div class="row">          
        <% (0..2).each do |column| %>
          <form method="post" action="play">
            <input type="hidden" name="cell" value="<%=row * 3 + column + 1%>">
            <button class="board-cell" type="submit"><%=matrix[row][column]%></button>
          </form>
        <% end %>
      </div>
    <% end%>
    </div>
    <div>Click in a cell to play.</div>
    <h1><%=game_result%></h1>
    <form method="post" action="/start">
      <button type="submit">
        reset game
      </button>
  </body>
</html>
